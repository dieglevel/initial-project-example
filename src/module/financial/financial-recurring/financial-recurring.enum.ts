export enum FINANCIAL_RECURRING_TYPE {
  SALARY = "salary",
  RENT = "rent",
  SUBSCRIPTION = "subscription",
  OTHER = "other",
}

export enum FINANCIAL_RECURRING_FREQUENCY {
  MONTHLY = "monthly",
  WEEKLY = "weekly",
  EVERY_N_DAYS = "every_n_days",
}
